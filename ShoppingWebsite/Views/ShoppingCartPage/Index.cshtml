@model ShoppingWebsite.Models.ViewModels.ShoppingCartViewModel

<div>
    This is cart
</div>
<div>
    @foreach (var item in Model.ProductIdsInCookie)
    {
        <h3>@item</h3>
    }
</div>
@foreach (var item in @Model.ShoppingCartPages)
{
    @item.Size
}
